{
  "name": "railway-casino",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build && cd .. && npm install --prefix server",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}