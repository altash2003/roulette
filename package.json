{
  "name": "railway-casino",
  "version": "1.0.0",
  "scripts": {
  "start": "node server/index.js",
  "build": "cd client && rm -rf node_modules && npm install && npm run build && cd .. && npm install --prefix server"
}
  "engines": {
    "node": "18.x"
  }

}
